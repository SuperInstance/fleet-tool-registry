[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "fleet_tool_registry"
version = "0.1.0"
description = "fleet-tool-registry — extracted from SuperInstance oracle1-workspace"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
